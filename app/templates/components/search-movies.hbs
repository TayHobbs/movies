<div class="row">
  <div class="col-sm-offset-9 col-sm-3">
    <div class="form-group">
        {{input class="form-control" value=searchField placeholder="Search"}}
    </div>
  </div>
</div>
{{#each movie in arrangedContent}}
  <div class="col-sm-2 movie" data-toggle="modal" data-target="#movie-detail" {{action "showDetail" movie}}>
    <div class="poster">
      <img {{bind-attr src="movie.posters.detailed"}} /><br>
    </div>
    <div class="title">
      {{movie.title}}
    </div>
    <div class="title">
      {{movie.ratings.critics_score}} {{movie.ratings.audience_score}}
    </div>
  </div>
{{else}}
  Nothing to see here
{{/each}}

{{movie-detail movie=movie}}
